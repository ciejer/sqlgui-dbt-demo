name: my_new_project
version: 1.0.0
config-version: 2
profile: default
source-paths:
  - models
analysis-paths:
  - analysis
test-paths:
  - tests
data-paths:
  - data
macro-paths:
  - macros
snapshot-paths:
  - snapshots
target-path: target
clean-targets:
  - target
  - dbt_modules
models:
  my_new_project:
    analytics:
      materialized: view
      film_category_name:
        +tags:
          - test1
          - test2
      sources:
        tags:
          - SourceTag
          - ShouldFail
      tags:
        - dvds
